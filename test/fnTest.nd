fn add(mut a: number, b: number) {
    output(<a> + <b>)
}

writeLn(<add(20, 10)>)

object anon is (mut a: number, b: number) => {
    output(<a> * <b>)
}

writeLn(<anon(10, 10)>)

writeLn("FNTEST END")